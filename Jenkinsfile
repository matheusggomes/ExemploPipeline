pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Certifique-se de que os comandos aqui estão corretos
                sh 'make'
            }
        }
    }
}
